{
  "name": "isometric_physics",
  "description": "Isometric Physics",
  "author": "Bruno Brito <brunodb3@gmail.com>",
  "private": false,
  "version": "0.0.1",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "yarn build",
    "start": "node -r ts-node/register/transpile-only -r tsconfig-paths/register dist/index.js",
    "start:dev": "ts-node-dev --clear -r tsconfig-paths/register src/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.14.1",
    "tslint": "^6.1.3",
    "typescript": "^4.6.3"
  }
}
